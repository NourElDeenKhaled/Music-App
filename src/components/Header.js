import React from "react";
import "../styles/App.css";
import SearchBar from "./SearchBar";

const Header = () => {
  return (
    <header>
      <SearchBar />
    </header>
  );
};

export default Header;
